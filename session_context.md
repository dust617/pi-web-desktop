# 当前上下文

## 目标

优先交付稳定、独立的 Pi Web Windows 桌面版；移动端在桌面版稳定后才开始。

## 关键决策

- Electron 壳 + 锁定的 Pi Web 运行时，而非包装浏览器或调用全局 `pi-web`。
- 服务只绑定 `127.0.0.1` 且使用动态端口，避免当前机器 30141 已占用的问题。
- 外部拖入文件是会话附件引用；项目文件浏览器只展示当前工作根目录。
- 更新为用户确认后的候选版本健康检查与可回退切换。
- Android 后续用 Tailscale 私有网络；首版复用远程会话和工具预设，不做独立公网服务。

## 不做

- 不接入 Qoder。
- 不把外部文件自动复制进项目。
- 不在移动端首版承诺逐条工具调用审批或复杂本地文件编辑。

