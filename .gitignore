# Dependencies
node_modules/
resources/pi-web/node_modules/

# Build output (TypeScript → JS)
dist/

# Backup & logs
.backup/
*.log

# Test & debug artifacts
.test/
.pi-subagents/
nul

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Mobile runtime state / secrets (never commit)
bff-pairing-code.txt
*-status.json
.ns-watchdog.lock/

# Build / package artifacts (large; produced by `npm run dist`)
release/

# Downloaded deploy-time binaries (fetch separately, not vendored)
resources/cloudflared/
