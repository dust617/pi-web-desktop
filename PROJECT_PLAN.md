# Pi Web Desktop 与移动端互联：项目计划

## 1. 目标与边界

### 目标

将已安装的 Pi Web 转为稳定的 Windows 桌面产品，消除“先开终端、再开浏览器”的使用成本，并补齐项目入口、外部文件引用和常用原生快捷操作。移动端在桌面稳定后，通过 Android + 私有组网安全地复用同一会话。

### 成功标准

1. 双击打开独立窗口，不要求用户先开终端或浏览器。
2. 读取既有 `~/.pi/agent` 会话和配置；不迁移、不破坏原有 Pi Web 或全局 npm 包。
3. 一个窗口内完成项目选择、会话继续、项目文件浏览与预览。
4. 任意本地文件或文件夹可拖入聊天，作为“临时外部附件”明确交给 Agent 阅读。
5. 上游 Pi Web 更新不会自动破坏正在使用的桌面版；用户可看到更新并在确认后安装已验证的兼容版本。

### 明确不做

- 不接入 Qoder、其他 Agent 或模型供应商。
- 不让外部附件自动复制、移动或删除用户文件。
- 不以移动端替代桌面端项目管理；移动端只做远程延续与查看。
- 不开放公网端口、不自建账号系统或公共中继服务。

## 2. 已锁定决策与架构

- 项目目录：`D:\PI-web-desktop`。
- 桌面技术：Electron，内置并锁定已验证的 Pi Web 运行时；不直接依赖或修改全局 npm 安装。
- 上游策略：发现新版本后由用户手动确认更新；先健康检查，再切换，失败保留现用版本。
- 外部文件：默认“临时引用，可导入项目”；不自动复制、不混入项目文件树。
- 移动端：最后实施 Android PWA/轻壳，通过 Tailscale 私有网络访问；不暴露公网端口。

```text
Pi Web Desktop（Electron）
  ├─ Main：窗口、托盘、项目选择、生命周期、诊断与更新协调
  ├─ Preload：最小化原生能力桥（附件、文件夹选择、在资源管理器显示）
  ├─ 受控 Pi Web Runtime：固定版本 + 小型桌面补丁
  │    └─ 127.0.0.1:动态空闲端口，仅本机访问
  └─ 用户既有 ~/.pi/agent：会话、模型与技能配置

后续 Android PWA/轻壳
  └─ Tailscale 私有网络 → 桌面端受认证网关 → 同一个 Pi Web Runtime
```

运行时采用“锁定、验证、回退”策略：桌面安装包内含经过验证的 Pi Web 与依赖锁定版本。不会调用全局 `pi-web`，也不会在启动时 `npm update`。上游新版本只作为可评估候选；完成兼容测试后，作为新的桌面运行时包发布。更新失败或新版本健康检查失败时，继续使用旧版本。

Pi Web 子进程使用 `--hostname 127.0.0.1` 和动态端口启动、关闭窗口时优雅停止。Electron 渲染窗口启用 sandbox 与 `contextIsolation`，禁用 `nodeIntegration`，只允许受信的本地地址加载，预加载桥逐项校验调用来源和路径。

## 3. 分阶段交付

### 阶段 0：技术验证与工程骨架（约 1 天）

- 创建 Electron + TypeScript 工程、锁定 Pi Web 源版本与依赖锁文件。
- 用 Electron 自带 Node 以子进程启动 Pi Web；为每次启动分配空闲回环端口，轮询 HTTP 200 后才显示窗口。
- 验证沿用现有 Pi 会话、模型配置和项目文件预览；验证端口占用、启动失败与子进程退出的可见错误提示。

通过标准：本机 30141 已占用时，桌面端仍能启动并显示同一批历史 Pi 会话。

### 阶段 1：桌面最小可用版（约 2 天）

- 打包 Windows 安装版；主窗口保存尺寸与最近状态，支持最小化到托盘、退出与重新启动。
- 提供“打开项目目录”“新建/继续会话”“显示当前项目目录”“刷新页面”的原生菜单或快捷入口。
- 继续使用 Pi Web 自有项目侧栏、会话树、Git worktree 与文件 Explorer；不重复造第二套项目管理器。
- 收敛启动日志和故障诊断：端口、运行时版本、子进程退出码，不记录密钥、消息正文或文件内容。

通过标准：无需浏览器/终端即可完成新建项目会话、继续历史会话、查看/预览项目文件和安全退出。

### 阶段 2：外部附件与项目工作台增强（约 2–3 天）

- 在 Pi Web 的桌面补丁中加入拖放捕获：图片沿用原生图片附件；其他文件、目录默认登记为临时外部附件。
- 每条外部附件记录会话 ID、规范化绝对路径、名称、类型、加入时间、可用状态；仅保存本机元数据，不复制文件。
- 聊天发送时将附件以清晰、可读的本机路径和“临时外部附件”说明附入提示，供 Pi 的工具按需读取；不伪装为项目文件。
- 在聊天附近增加“外部附件”虚拟分组，提供移除引用、在资源管理器显示、检查路径是否仍存在、按用户确认导入当前项目等操作。
- “导入项目”是显式复制行为，目标为当前项目的 `.pi-web-desktop/attachments/`；发生重名时让用户选择新名称或取消，目录导入先显示文件数和总大小。
- 文件 Explorer 保持只针对项目根目录；增加“附加当前选中文件”和“在资源管理器显示”快捷动作，不在 MVP 中另做任意文件删除/批量重命名。

通过标准：将项目外的文档或文件夹拖入聊天后，Agent 能明确读取；项目树不被污染；原文件删除/移动后附件显示失效而不会导致应用崩溃。

### 阶段 3：稳定性、更新与发布（约 2 天）

- 为启动、健康检查、窗口崩溃恢复、异常退出和清理子进程建立自动化测试与人工冒烟清单。
- “检查 Pi Web 更新”只获取上游版本信息并展示变更；“更新”只安装已由本项目验证并打包的运行时包，先做临时健康探测，再原子切换，保留上一个可用版本。
- 生成安装包和可选便携版；应用内显示桌面壳版本、锁定 Pi Web 版本与回退入口。
- 在干净 Windows 用户环境、端口冲突、离线启动、已有全局 Pi Web 和多个项目会话的情形下回归测试。

通过标准：用户可拒绝更新并稳定使用旧版；候选运行时不能启动时自动保持旧版，历史会话和用户 Pi 配置不丢失。

### 阶段 4：Android 私有互联（桌面稳定后，约 5–8 天）

- 前置条件：桌面版已稳定；桌面和 Android 均加入同一 Tailscale tailnet，启用 MagicDNS 与可信 HTTPS 证书。
- 桌面端增加独立远程网关：仅监听 Tailscale 接口，不直接暴露原始 Pi Web 端口；使用二维码配对、可撤销设备令牌和会话过期。
- Android 首版为可安装 PWA/轻壳，复用响应式 Pi Web 界面和同一会话：查看历史、流式任务、发送/继续消息、停止任务、查看外部附件元数据和上传小文件。
- 远程端展示当前项目、会话与既有工具预设；首版不实现独立的逐条 Bash/写文件审批机制，不下载桌面任意文件，也不执行桌面端更新。
- 断线后自动重连并以消息 ID 去重；桌面端可随时撤销某一手机设备或关闭远程网关。

通过标准：在非同一局域网但同一 Tailscale 网络下，Android 可安全继续桌面当前项目会话；关闭远程功能或撤销设备后无法再访问。

## 4. 验收、风险与工期

### 验收测试

- 生命周期：动态端口、端口冲突、重复启动、Pi Web 子进程崩溃、桌面端退出后无残留服务。
- 附件：单文件、目录、图片、无权限路径、已移动路径、重名导入、大目录取消导入。
- 隔离：网页内容不能获得任意 Node/系统权限；每个原生 IPC 调用校验来源、参数和当前窗口。
- 更新：无网络、用户取消、候选包校验失败、候选运行时健康检查失败、回退后会话可继续。
- 移动端（后续）：未配对设备、令牌撤销、Tailscale 断线、桌面休眠、双端同时发送消息与恢复后去重。

### 工期

- 桌面最小可用版：约 3 个工作日（阶段 0–1）。
- 桌面稳定可长期使用版：约 7–9 个工作日（阶段 0–3）。
- 移动端在桌面稳定验收后单独投入约 5–8 个工作日。

### 当前实施起点

建立独立工程骨架，验证 Electron 能以动态回环端口启动锁定版本的 Pi Web，并由窗口正常加载。

